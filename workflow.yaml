# yaml-language-server: $schema=https://raw.githubusercontent.com/codemod/codemod/refs/heads/main/schemas/workflow.json

version: "1"

nodes:
  - id: bytestrone-ef-migration-assessment
    name: "EF6 to EF Core Migration Assessment"
    type: automatic
    steps:
      - name: "Run C# Pattern Mining"
        codemod:
          # Local sibling path — both packages live side-by-side in this workspace
          # and aren't published to a registry. Swap for a registry reference
          # (e.g. "bytestrone-ef-csharp-pattern-mining@1.1.0") once published.
          source: "../bytestrone-ef-csharp-pattern-mining"

